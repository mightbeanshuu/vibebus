{
  "name": "vibebus",
  "version": "2.0.0",
  "description": "A real-time, bidirectional MCP bus that lets CLI coding agents message, wake, question, and coordinate with each other.",
  "type": "module",
  "bin": {
    "vibebus-mcp": "./bin/vibebus-mcp.js",
    "vibebus": "./bin/vibebus.js",
    "cli-team-mcp": "./bin/vibebus-mcp.js",
    "cli-team": "./bin/vibebus.js"
  },
  "scripts": {
    "build": "node --check bin/vibebus-mcp.js && node --check bin/vibebus.js && node --check src/mcp-server.js && node --check src/mcp-features.js && node --check src/store.js && node --check src/tools.js && node --check src/shared.js && node --check src/events.js && node --check src/watcher.js && node --check src/presence.js && node --check src/agents.js && node --check src/messaging.js && node --check src/tasks.js && node --check src/asks.js && node --check src/errors.js && node --check src/session.js && node --check src/pump.js && node --check src/dashboard.js && node --check skills/vibebus/scripts/register-session.js",
    "test": "node --test"
  },
  "keywords": [
    "mcp",
    "cli",
    "agents",
    "coordination",
    "codex",
    "claude",
    "gemini",
    "antigravity",
    "grok",
    "windsurf",
    "qwen",
    "aider",
    "goose",
    "handoff",
    "skill",
    "realtime",
    "bidirectional",
    "sampling",
    "elicitation",
    "multi-agent",
    "orchestration",
    "wake"
  ],
  "author": "Anshu <amananshu2004@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "src",
    "skills",
    "assets",
    "docs",
    "README.md",
    "LICENSE"
  ]
}
